node_modules
dist
android
# iOS — commit native project + custom plugins; ignore cap sync build output
ios/App/App/public/
ios/App/Pods/
ios/capacitor-cordova-ios-plugins/
ios/**/DerivedData/
.DS_Store
*.local
.env

# Local Netlify folder
.netlify

# Xcode build cache / DerivedData redirected to project root
/build/

# Stray duplicate Cordova config files (created by repeated cap sync runs)
ios/App/App/config*.xml

# Unused local asset experiments
/kenney_isometricBlocks/
